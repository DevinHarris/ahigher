import React from 'react';
import Nav from './Nav';
import SearchBar from './SearchBar';

const Header = () => (
    <header className="header">
        <Nav />
    </header>
)

export default Header;